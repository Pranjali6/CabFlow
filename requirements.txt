# CabFlow dashboard runtime dependencies.
# This is the slim set used by Streamlit Community Cloud. The full
# research deps (torch, prophet, neuralforecast, shap, mlflow, fastapi,
# etc.) live in requirements-dev.txt - they are not needed to run the
# deployed dashboard.

streamlit>=1.29.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# Plotting + dashboards
plotly>=5.18.0

# Config + utilities
pyyaml>=6.0.0
certifi>=2024.0.0

# Models the dashboard unpickles
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# Statistical analysis tab (ADF, ACF, PACF, STL) + scipy stats
statsmodels>=0.14.0
scipy>=1.11.0

# Feature engineering (holiday flags + Open-Meteo refresh)
holidays>=0.40

# Optional - enables the "Agent Insights" tab if ANTHROPIC_API_KEY is set
anthropic>=0.39.0
